# Baseline de deriva conhecida entre as duas cópias do pipeline (raiz x plugins/btt-sdd/).
#
# Lido por `scripts/check-plugin-sync.py`. É a escotilha de escape para deriva que não dá para
# ressincronizar no mesmo PR em que foi descoberta: o par entra aqui, o gate passa a reclamar só de
# deriva NOVA, e a linha sai quando a ressincronização acontece.
#
# Formato: <rótulo do arquivo da raiz> # <motivo/estado + issue que vai resolver>
#
# Duas regras que impedem este arquivo de virar um tapete:
#   1. Um par listado aqui que voltou a estar em dia faz o script FALHAR — a linha precisa sair.
#   2. Toda linha tem de apontar a issue que vai resolvê-la. Nenhuma divergência mora aqui para
#      sempre: se ela é deliberada, o lugar dela é `DELIBERADAS` no script, não aqui.
#
# ESTADO ATUAL: vazia. Os 11 pares registrados quando o script nasceu (issue #281) foram
# ressincronizados pela issue #290 — 9 por padronização de redação das referências cruzadas e
# reposição de conteúdo perdido, 2 registrados como divergência deliberada em `DELIBERADAS`
# (`create-project`, cuja resolução do caminho do scaffold depende do canal de distribuição, e
# `hotfix`, que referencia um template exclusivo deste repositório).
#
# Nenhuma entrada abaixo desta linha.
